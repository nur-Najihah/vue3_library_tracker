{
  "name": "json-server-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for Library Tracker",
  "scripts": {
    "start": "json-server --watch /app/books.json --host 0.0.0.0 --port 3000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
